# MCP Feature Flags Tools — Feature Toggles for AI Agents

> MCP server for feature flags. 16 tools, zero deps, pure Python stdlib.

## Tools
create_flag, is_enabled, enable, disable, toggle, set_rollout, add_target, remove_targets, set_variants, get_variant, get_flag, list_flags, delete_flag, get_history, stats, reset

## Tests
```bash
python -m pytest tests/ -v  # 32 tests, all passing
```

## License
MIT — AMEOBIUS
